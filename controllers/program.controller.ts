import { Program } from "../models";
import { ProgramNS } from "../types";


const addProgram = (program: ProgramNS.Program) => {
    const newProgram = new Program({

    });

};

export default {
    addProgram,
};